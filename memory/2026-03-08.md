# 2026-03-08

## User & workflow preferences
- User (Кирилл) prefers fast iterative design updates with direct blunt feedback and immediate implementation.
- User explicitly requested a reviewer-loop workflow (worker + QA reviewer behavior) for future tasks.
- User requested strict visual alignment to a reference site and dislikes “same page with only font changes”.

## Skills and setup actions completed
- Installed skills:
  - `find-skills`
  - `proactive-agent` (manual zip install from ClawHub page)
  - `humanizer` (manual zip install from ClawHub page)
  - `frontend-design` (manual zip install from ClawHub page)
  - `ui-ux-design`
  - `design-system-creation`
- Activated `proactive-agent` assets into workspace root and saved backups in `backup/proactive-agent-*`.
- Restarted/verified OpenClaw gateway status after skill changes.

## Infrastructure/tunnel
- Installed Tuna (`tuna.am`) and saved token.
- Active public tunnel configured to `https://vizard.ru.tuna.am` forwarding to local `127.0.0.1:8080`.
- Local web serving set from `/root/.openclaw/workspace/myclawbot-redesign` on port 8080.

## Website project state
- Project path: `/root/.openclaw/workspace/myclawbot-redesign`.
- Main live page: `/` (index + style) repeatedly redesigned toward reference parity with red theme.
- Variants `/1..../10` were created during exploration; later main page was consolidated and iterated.
- Major requested features implemented on main page:
  - One-price offer structure (`3 890 ₽/мес`).
  - Interactive model selector (Claude / GPT / Gemini).
  - Telegram connect flow with modal instructions and token input (`/newbot` steps).
  - Google login CTA inside setup form.
  - “Что умеет OpenClaw?” section rebuilt to closer original structure in red tones.
  - Header adjusted to `MyClawBot.ru` + `Телеграм-канал`.
  - Disabled page zoom via viewport meta.
  - Capability chips now in 5 animated rows with alternating directions; icon style forced white.

## User-facing design constraints learned
- Must mirror original structure closely, but keep red brand palette.
- No WhatsApp block in the setup section.
- Card/CTA spacing and type scale are critical; user notices misalignment immediately.
- Capability block should feel like the original (structured rows/chips, clear visual hierarchy).

## Git history milestones (same day)
- Multiple commits were made for incremental redesign, interactions, tunnel fixes, and section rebuilds.
- Latest capability animation update commit: `b98973d`.

## Astro migration + UI consistency (late evening)
- Created Astro project baseline at `/root/.openclaw/workspace/myclawbot-redesign-astro` and migrated key routes from static `public` pages into Astro routes:
  - `/` (homepage)
  - `/guides/`
  - `/contact/`
  - `/privacy-policy/`
  - `/terms-of-service/`
  - `/cookie-policy/`
  - `/refund-policy/`
  - `/login/`
  - `/register/`
- Kept other pages/assets available while migrating incrementally (hybrid approach: Astro routes + remaining static assets).
- Disabled Astro Dev Toolbar in `astro.config.mjs` (`devToolbar.enabled=false`) after user reported the settings panel.

## Brand/style decisions locked in
- User clarified repeated preference: keep strict visual consistency with brand reds, but avoid forcing everything into button-gradient style.
- In capabilities block (“Что умеет OpenClaw?”): restored animated rows and 3 chip variants (gradient / plain / dashed), with gradient in brand red family.
- In subscription includes block (“Что входит в подписку?”): matched card gradient to pricing form panel when requested.
- Header/login CTA refinements:
  - Homepage “Войти” changed to red gradient + left white filled icon.
  - Login page “На главную” changed to red gradient + left white filled home icon.
  - Matched login header dimensions/divider/font-weight to homepage header for consistency.

## Mobile/iOS issues and fixes
- User repeatedly reported horizontal side-swipe and top/bottom white bars on iOS.
- Implemented stronger anti-horizontal-pan CSS guards (`overflow-x`, `overscroll-behavior-x`, `touch-action`) and marquee containment tweaks.
- Added/standardized iOS safe-area paint fixes on affected pages (notably login/cabinet), including explicit black top safe-area overlay and black theme-color meta where needed.

## Collaboration preference reinforced
- User gives rapid, visual, iterative feedback (screenshot-driven), expects immediate direct fixes without extra explanation.
- Priority for next sessions: keep edits small, targeted, and verify exact block/page before broad style changes.
