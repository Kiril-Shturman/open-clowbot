---
---

<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Центр миссий — Subagents</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600;700&family=Press+Start+2P&display=swap" rel="stylesheet">
    <style>
      :root {
        --bg: #0b0d12;
        --panel: #11161f;
        --panel-2: #171d28;
        --line: rgba(255,255,255,.08);
        --text: #f5f7fb;
        --muted: #98a3b8;
        --danger: #ff5662;
        --danger-2: #ab1837;
        --ok: #63f59b;
        --warn: #ffcf57;
        --cyan: #70ddff;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        color: var(--text);
        font-family: 'IBM Plex Sans', system-ui, sans-serif;
        background:
          radial-gradient(circle at top left, rgba(255,86,98,.18), transparent 24%),
          radial-gradient(circle at top right, rgba(112,221,255,.12), transparent 20%),
          linear-gradient(180deg, #0b0d12 0%, #080a0f 100%);
      }
      .wrap { width: min(1440px, calc(100% - 28px)); margin: 0 auto; padding: 20px 0 40px; }
      .card, .topbar {
        background: linear-gradient(180deg, rgba(255,255,255,.04), rgba(255,255,255,.02));
        border: 1px solid var(--line);
        border-radius: 24px;
        box-shadow: 0 24px 80px rgba(0,0,0,.36);
      }
      .topbar {
        display: flex; justify-content: space-between; align-items: center; gap: 16px;
        padding: 16px 18px; margin-bottom: 16px;
      }
      .brand { display: flex; gap: 14px; align-items: center; }
      .mark { width: 54px; height: 54px; border-radius: 18px; display: grid; place-items: center; background: linear-gradient(135deg, var(--cyan), #17789b); color: #091118; font-family: 'Press Start 2P', monospace; font-size: 11px; }
      .eyebrow { font-size: 11px; letter-spacing: .08em; text-transform: uppercase; color: #9cecff; font-weight: 800; }
      h1 { margin: 6px 0 4px; font-size: clamp(26px, 4vw, 42px); line-height: .98; }
      .subtitle { margin: 0; color: var(--muted); max-width: 760px; }
      .actions { display: flex; gap: 10px; flex-wrap: wrap; }
      .btn {
        border: 1px solid rgba(255,255,255,.08); border-radius: 14px; padding: 12px 14px;
        background: rgba(255,255,255,.04); color: #fff; font-weight: 700; cursor: pointer;
      }
      .btn.primary { background: linear-gradient(135deg, var(--cyan), #17789b); color: #091118; border-color: transparent; }
      .btn.warn { background: linear-gradient(135deg, #ffcf57, #b27a08); color: #161103; border-color: transparent; }
      .card { padding: 18px; }
      .section-head { display: flex; justify-content: space-between; align-items: center; gap: 12px; margin-bottom: 14px; }
      .section-title { font-size: 20px; font-weight: 800; }
      .tag { display: inline-flex; padding: 6px 10px; border-radius: 999px; font-size: 12px; font-weight: 700; background: rgba(255,255,255,.06); }
      .tag.live { background: rgba(99,245,155,.14); color: #c8fbd9; }
      .tag.cyan { background: rgba(112,221,255,.14); color: #c7f2ff; }
      .tag.warn { background: rgba(255,207,87,.14); color: #ffe7a6; }
      .connect-grid, .hero-grid, .flow, .subagents, .lower-grid, .stat-grid, .two { display: grid; gap: 16px; }
      .connect-grid { grid-template-columns: 1fr 1fr; margin-bottom: 16px; }
      .hero-grid { grid-template-columns: 1.15fr .85fr; margin-bottom: 16px; }
      .flow { grid-template-columns: 1fr 1fr 1fr; margin-bottom: 16px; }
      .subagents { grid-template-columns: repeat(3, minmax(0, 1fr)); margin-bottom: 16px; }
      .lower-grid { grid-template-columns: 1fr 1fr 1fr; }
      .stat-grid { grid-template-columns: repeat(4, minmax(0,1fr)); margin-bottom: 14px; }
      .two { grid-template-columns: 1fr 1fr; }
      .mission-title { font-size: clamp(28px, 4vw, 44px); font-weight: 800; line-height: .98; margin-bottom: 14px; }
      .stat, .panel, .agent, .item { padding: 14px; border-radius: 18px; background: var(--panel-2); border: 1px solid rgba(255,255,255,.06); }
      .label { color: var(--muted); font-size: 13px; }
      .value { font-size: 26px; font-weight: 800; margin-top: 6px; }
      .panel strong { display: block; margin-bottom: 6px; }
      .agent-name { font-size: 16px; font-weight: 800; }
      .agent-role { margin-top: 4px; color: var(--muted); font-size: 13px; }
      .agent-task { margin-top: 12px; font-size: 14px; line-height: 1.35; }
      .agent-status { margin-top: 10px; color: var(--muted); font-size: 12px; }
      .item-title { font-size: 15px; font-weight: 700; }
      .item-meta { margin-top: 6px; color: var(--muted); font-size: 13px; line-height: 1.4; }
      .textarea, .select, .input { width: 100%; min-height: 54px; border-radius: 14px; border: 1px solid rgba(255,255,255,.08); background: rgba(255,255,255,.04); color: #fff; padding: 12px 14px; font: inherit; }
      .textarea { min-height: 96px; resize: vertical; }
      .form { display: grid; gap: 10px; }
      .status-ok { color: #c8fbd9; }
      .status-warn { color: #ffe7a6; }
      @media (max-width: 1180px) {
        .connect-grid, .hero-grid, .flow, .subagents, .lower-grid, .stat-grid, .two { grid-template-columns: 1fr; }
      }
      @media (max-width: 760px) {
        .wrap { width: min(100% - 18px, 1440px); }
        .topbar { flex-direction: column; align-items: stretch; }
        .actions { width: 100%; }
        .btn { width: 100%; }
      }
    </style>
  </head>
  <body>
    <main class="wrap">
      <section class="topbar">
        <div class="brand">
          <div class="mark">SA</div>
          <div>
            <div class="eyebrow">Subagent model</div>
            <h1>Центр миссий — через саб-агентов</h1>
            <p class="subtitle">Сначала можно подключить текущий ClawBot, либо внешний runtime. Потом ставим цель, и COO начинает гонять саб-агентов.</p>
          </div>
        </div>
        <div class="actions">
          <a class="btn" href="/mission-control/">Старая версия</a>
          <button class="btn" id="tickBtn">Шаг цикла</button>
          <button class="btn warn" id="blockerBtn">Смоделировать blocker</button>
          <button class="btn primary" id="startBtn">START</button>
        </div>
      </section>

      <section class="connect-grid">
        <article class="card">
          <div class="section-head"><div class="section-title">0A. Подключить текущий ClawBot</div><span class="tag cyan">Current runtime</span></div>
          <div class="panel"><strong>Для быстрого старта</strong> Этот вариант нужен, если хочешь использовать текущий контур как главный COO/runtime без ручного ввода URL.</div>
          <div class="two" style="margin-top:12px;">
            <button class="btn primary" id="connectCurrentBtn">Подключить текущий ClawBot</button>
            <div class="panel"><strong>Статус</strong><span id="currentConnectionStatus">Не подключён</span></div>
          </div>
        </article>

        <article class="card">
          <div class="section-head"><div class="section-title">0B. Подключить внешний ClawBot</div><span class="tag cyan">External runtime</span></div>
          <div class="form">
            <input class="input" id="runtimeName" value="Worker ClawBot" placeholder="Название подключения" />
            <input class="input" id="runtimeUrl" value="https://your-runtime.example.com" placeholder="URL runtime" />
            <input class="input" id="runtimeToken" value="" placeholder="API token / secret" />
            <div class="two">
              <button class="btn primary" id="testConnectionBtn">Проверить подключение</button>
              <button class="btn" id="saveConnectionBtn">Сохранить подключение</button>
            </div>
          </div>
        </article>
      </section>

      <section class="card" style="margin-bottom:16px;">
        <div class="section-head"><div class="section-title">Статус подключений</div><span class="tag" id="connectionTag">Not connected</span></div>
        <div class="panel"><strong>Главный контур</strong><span id="connectionLine">Подключи либо текущий ClawBot, либо внешний runtime.</span></div>
        <div class="panel" style="margin-top:12px;"><strong>Что проверяем</strong><span id="connectionChecks">1. Доступен ли runtime. 2. Валиден ли token. 3. Может ли runtime работать как control plane для миссий.</span></div>
      </section>

      <section class="hero-grid">
        <article class="card">
          <div class="section-head"><div class="section-title">1. Главная миссия</div><span class="tag live" id="missionTag">Waiting connect</span></div>
          <div class="mission-title" id="missionTitle">Собрать 10 кейсов использования OpenClaw для предпринимателей</div>
          <div class="stat-grid">
            <div class="stat"><div class="label">Commander</div><div class="value">COO</div></div>
            <div class="stat"><div class="label">Subagents</div><div class="value" id="subagentCount">3</div></div>
            <div class="stat"><div class="label">Активный шаг</div><div class="value" id="stepValue">0/3</div></div>
            <div class="stat"><div class="label">Статус</div><div class="value" id="statusValue">Ожидает подключения</div></div>
          </div>
          <div class="panel"><strong>Что делает COO</strong><span id="cooLine">COO ждёт, пока будет подключён главный runtime.</span></div>
          <div class="panel" style="margin-top:12px;"><strong>Следующий шаг</strong><span id="nextStep">Подключить текущий ClawBot или внешний runtime.</span></div>
        </article>

        <article class="card">
          <div class="section-head"><div class="section-title">2. Запуск миссии</div><span class="tag cyan">Founder input</span></div>
          <div class="form">
            <textarea class="textarea" id="missionInput">Собери 10 сильных кейсов использования OpenClaw для предпринимателей и упакуй их в понятные идеи для сайта и Reels.</textarea>
            <div class="two">
              <select class="select" id="missionType">
                <option>Контент</option>
                <option>Ресерч</option>
                <option>Продукт</option>
                <option>Продажи</option>
              </select>
              <select class="select" id="missionPriority">
                <option>Высокий приоритет</option>
                <option>Средний приоритет</option>
                <option>Низкий приоритет</option>
              </select>
            </div>
          </div>
          <div class="panel" style="margin-top:12px;"><strong>Порядок</strong> 1. Подключить один из runtime-ов. 2. Поставить цель. 3. Нажать START. 4. Смотреть, как COO ведёт саб-агентов.</div>
        </article>
      </section>

      <section class="card" style="margin-bottom:16px;">
        <div class="section-head"><div class="section-title">3. Как течёт работа</div><span class="tag">Простой контур</span></div>
        <div class="flow">
          <div class="panel"><strong>Founder</strong><span id="flowFounder">Даёт цель и приоритет.</span></div>
          <div class="panel"><strong>COO agent</strong><span id="flowCoo">Разбивает на шаги и выбирает, кого спавнить первым.</span></div>
          <div class="panel"><strong>Subagents</strong><span id="flowSubagents">Делают узкие задачи: research, content, build.</span></div>
        </div>
      </section>

      <article class="card" style="margin-bottom:16px;">
        <div class="section-head"><div class="section-title">4. Кто сейчас работает</div><span class="tag cyan">Execution layer</span></div>
        <div class="subagents" id="subagentsList"></div>
      </article>

      <section class="lower-grid">
        <article class="card">
          <div class="section-head"><div class="section-title">Что уже вернулось</div><span class="tag live">Artifacts</span></div>
          <div class="list" id="artifactsList"></div>
        </article>
        <article class="card">
          <div class="section-head"><div class="section-title">Проблемы</div><span class="tag">Blockers</span></div>
          <div class="list" id="blockersList"></div>
        </article>
        <article class="card">
          <div class="section-head"><div class="section-title">Лента COO</div><span class="tag cyan">Mission log</span></div>
          <div class="list" id="logList"></div>
        </article>
      </section>
    </main>

    <script>
      const STORAGE_KEY = 'mission-control-subagents-v3';
      const templates = {
        Контент: [
          { name: 'Research Subagent', role: 'Поиск и анализ', task: 'Собирает кейсы и паттерны', output: 'Список кейсов и инсайтов' },
          { name: 'Content Subagent', role: 'Контент и упаковка', task: 'Превращает кейсы в темы и hooks', output: 'Темы для сайта и Reels' },
          { name: 'Builder Subagent', role: 'Сборка результата', task: 'Упаковывает финальный артефакт', output: 'Структура страницы / финальный драфт' },
        ],
        Ресерч: [
          { name: 'Research Subagent', role: 'Поиск и анализ', task: 'Поднимает источники и сравнения', output: 'Таблица и summary' },
          { name: 'Synthesis Subagent', role: 'Синтез', task: 'Собирает выводы и паттерны', output: 'Ключевые выводы' },
          { name: 'Packager Subagent', role: 'Упаковка', task: 'Собирает итоговый отчёт', output: 'Готовый документ' },
        ],
        Продукт: [
          { name: 'Research Subagent', role: 'Ресерч', task: 'Собирает входные требования', output: 'Список требований' },
          { name: 'Builder Subagent', role: 'Разработка', task: 'Готовит реализацию / прототип', output: 'MVP / код' },
          { name: 'QA Subagent', role: 'Проверка', task: 'Проверяет результат и слабые места', output: 'Список доработок' },
        ],
        Продажи: [
          { name: 'Lead Subagent', role: 'Лиды', task: 'Ищет подходящих лидов', output: 'Lead list' },
          { name: 'Offer Subagent', role: 'Оффер', task: 'Готовит оффер и сообщения', output: 'Outreach pack' },
          { name: 'Closer Subagent', role: 'Финализация', task: 'Собирает итог и next actions', output: 'Готовый sales handoff' },
        ],
      };

      const defaultState = {
        currentConnection: { status: 'offline', line: 'Текущий ClawBot ещё не подключён.' },
        externalConnection: { name: 'Worker ClawBot', url: 'https://your-runtime.example.com', token: '', status: 'offline' },
        activeConnection: null,
        missionTitle: 'Собрать 10 кейсов использования OpenClaw для предпринимателей',
        missionType: 'Контент',
        missionPriority: 'Высокий приоритет',
        status: 'Ожидает подключения',
        step: 0,
        cooLine: 'COO ждёт, пока будет подключён главный runtime.',
        nextStep: 'Подключить текущий ClawBot или внешний runtime.',
        agents: templates['Контент'].map((a) => ({ ...a, status: 'Ожидание' })),
        artifacts: [],
        blockers: [],
        log: ['Система ждёт подключения ClawBot runtime.'],
      };

      let state = loadState();
      function loadState() {
        try {
          const raw = localStorage.getItem(STORAGE_KEY);
          return raw ? JSON.parse(raw) : structuredClone(defaultState);
        } catch {
          return structuredClone(defaultState);
        }
      }
      function saveState() { localStorage.setItem(STORAGE_KEY, JSON.stringify(state)); }
      function $(id) { return document.getElementById(id); }
      function pushLog(text) { state.log.unshift(text); state.log = state.log.slice(0, 10); }

      function activeConnectionText() {
        if (state.activeConnection === 'current') return 'Текущий ClawBot подключён и выбран главным контуром.';
        if (state.activeConnection === 'external') return `Внешний runtime «${state.externalConnection.name}» подключён и выбран главным контуром.`;
        return 'Подключи либо текущий ClawBot, либо внешний runtime.';
      }

      function render() {
        $('runtimeName').value = state.externalConnection.name;
        $('runtimeUrl').value = state.externalConnection.url;
        $('runtimeToken').value = state.externalConnection.token;
        $('currentConnectionStatus').textContent = state.currentConnection.status === 'online' ? 'Подключён' : 'Не подключён';
        $('connectionTag').textContent = state.activeConnection ? 'Connected' : 'Not connected';
        $('connectionTag').className = `tag ${state.activeConnection ? 'live' : ''}`;
        $('connectionLine').innerHTML = state.activeConnection
          ? `<span class="status-ok">${activeConnectionText()}</span>`
          : activeConnectionText();
        $('connectionChecks').textContent = 'Проверяем доступность runtime, валидность token и пригодность runtime для роли control plane.';

        $('missionTitle').textContent = state.missionTitle;
        $('statusValue').textContent = state.status;
        $('stepValue').textContent = `${state.step}/${state.agents.length}`;
        $('cooLine').textContent = state.cooLine;
        $('nextStep').textContent = state.nextStep;
        $('subagentCount').textContent = String(state.agents.length);
        $('missionTag').textContent = state.status === 'Готово' ? 'Mission complete' : state.activeConnection ? 'COO active' : 'Waiting connect';
        $('flowFounder').textContent = `${state.missionType} • ${state.missionPriority}`;
        $('flowCoo').textContent = state.cooLine;
        $('flowSubagents').textContent = state.agents.map((a) => a.name).join(', ');

        $('subagentsList').innerHTML = state.agents.map((agent) => `
          <div class="agent">
            <div class="agent-name">${agent.name}</div>
            <div class="agent-role">${agent.role}</div>
            <div class="agent-task">${agent.task}</div>
            <div class="agent-status">Статус: ${agent.status}</div>
          </div>
        `).join('');

        $('artifactsList').innerHTML = state.artifacts.length
          ? state.artifacts.map((a) => `<div class="item"><div class="item-title">${a.title}</div><div class="item-meta">${a.body}</div></div>`).join('')
          : '<div class="item"><div class="item-title">Пока пусто</div><div class="item-meta">Артефакты появятся после запуска миссии и работы саб-агентов.</div></div>';

        $('blockersList').innerHTML = state.blockers.length
          ? state.blockers.map((b) => `<div class="item"><div class="item-title">${b.title}</div><div class="item-meta">${b.body}</div></div>`).join('')
          : '<div class="item"><div class="item-title">Пока нет blocker-ов</div><div class="item-meta">COO пока держит миссию без эскалаций.</div></div>';

        $('logList').innerHTML = state.log.map((line) => `<div class="item"><div class="item-title">${line}</div></div>`).join('');
        saveState();
      }

      function connectCurrent() {
        state.currentConnection.status = 'online';
        state.activeConnection = 'current';
        if (state.status === 'Ожидает подключения') {
          state.status = 'Готов к старту';
          state.cooLine = 'COO видит текущий ClawBot как главный контур и готов запускать миссию.';
          state.nextStep = 'Нажать START и отдать первую миссию.';
        }
        pushLog('Текущий ClawBot подключён как главный runtime.');
        render();
      }

      function testExternal() {
        state.externalConnection.name = $('runtimeName').value.trim() || 'Worker ClawBot';
        state.externalConnection.url = $('runtimeUrl').value.trim();
        state.externalConnection.token = $('runtimeToken').value.trim();
        if (!state.externalConnection.url || !state.externalConnection.token) {
          state.externalConnection.status = 'error';
          pushLog('Проверка внешнего runtime не прошла: не заполнен URL или token.');
          render();
          return;
        }
        state.externalConnection.status = 'online';
        state.activeConnection = 'external';
        if (state.status === 'Ожидает подключения') {
          state.status = 'Готов к старту';
          state.cooLine = `COO видит внешний runtime «${state.externalConnection.name}» и готов запускать миссию.`;
          state.nextStep = 'Нажать START и отдать первую миссию.';
        }
        pushLog(`Внешний runtime «${state.externalConnection.name}» успешно подключён.`);
        render();
      }

      function saveExternal() {
        state.externalConnection.name = $('runtimeName').value.trim() || 'Worker ClawBot';
        state.externalConnection.url = $('runtimeUrl').value.trim();
        state.externalConnection.token = $('runtimeToken').value.trim();
        pushLog(`Подключение внешнего runtime «${state.externalConnection.name}» сохранено.`);
        render();
      }

      function startMission() {
        if (!state.activeConnection) {
          pushLog('Нельзя стартовать миссию без подключённого runtime.');
          render();
          return;
        }
        const type = $('missionType').value;
        const priority = $('missionPriority').value;
        const title = $('missionInput').value.trim() || 'Новая миссия';
        state.missionTitle = title;
        state.missionType = type;
        state.missionPriority = priority;
        state.status = 'В работе';
        state.step = 1;
        state.cooLine = state.activeConnection === 'current'
          ? 'COO отправил первую подзадачу в текущий ClawBot runtime и активировал первого саб-агента.'
          : `COO отправил первую подзадачу во внешний runtime «${state.externalConnection.name}» и активировал первого саб-агента.`;
        state.nextStep = `Дождаться результата от ${templates[type][0].name}.`;
        state.agents = templates[type].map((a, i) => ({ ...a, status: i === 0 ? 'Активен' : 'В очереди' }));
        state.artifacts = [];
        state.blockers = [];
        state.log = [`Founder поставил миссию: ${title}`, `COO выбрал контур: ${state.activeConnection === 'current' ? 'текущий ClawBot' : state.externalConnection.name}.`, `Активирован ${templates[type][0].name}.`];
        render();
      }

      function tickCycle() {
        if (!state.activeConnection || state.status === 'Готово' || state.status === 'Ожидает подключения') return;
        const activeIndex = state.agents.findIndex((a) => a.status === 'Активен');
        if (activeIndex === -1) return;
        const active = state.agents[activeIndex];
        active.status = 'Завершил';
        state.artifacts.unshift({ title: `${active.name} вернул результат`, body: active.output });
        pushLog(`${active.name} завершил шаг и вернул артефакт COO.`);

        const next = state.agents[activeIndex + 1];
        if (next) {
          next.status = 'Активен';
          state.step = activeIndex + 2;
          state.nextStep = `Дождаться результата от ${next.name}.`;
          state.cooLine = `COO принял артефакт от ${active.name} и передал эстафету ${next.name}.`;
          pushLog(`COO активировал ${next.name}.`);
        } else {
          state.status = 'Готово';
          state.step = state.agents.length;
          state.nextStep = 'Миссия завершена. Можно запускать следующую.';
          state.cooLine = 'COO собрал все артефакты и закрыл миссию.';
          pushLog('COO завершил миссию и собрал финальный итог.');
        }
        render();
      }

      function addBlocker() {
        if (!state.activeConnection) return;
        const active = state.agents.find((a) => a.status === 'Активен');
        const who = active ? active.name : 'Unknown Subagent';
        state.blockers.unshift({ title: `${who}: blocker`, body: 'Нужен дополнительный контекст / входные данные для продолжения.' });
        state.blockers = state.blockers.slice(0, 5);
        state.cooLine = `COO получил blocker от ${who} и должен решить — уточнить задачу или перераспределить работу.`;
        state.nextStep = `Разобрать blocker от ${who} и дать новый приказ.`;
        pushLog(`${who} поднял blocker. COO ждёт решения.`);
        render();
      }

      $('connectCurrentBtn').addEventListener('click', connectCurrent);
      $('testConnectionBtn').addEventListener('click', testExternal);
      $('saveConnectionBtn').addEventListener('click', saveExternal);
      $('startBtn').addEventListener('click', startMission);
      $('tickBtn').addEventListener('click', tickCycle);
      $('blockerBtn').addEventListener('click', addBlocker);
      render();
    </script>
  </body>
</html>
